# ML Fundamentals

Implementations of machine-learning models for learning and experimentation.

- `src/ml_fundamentals/models_from_scratch.py`: implementations using only
  Python's standard library
- `src/ml_fundamentals/models_with_numpy.py`: equivalent implementations using
  NumPy

Install the project and create its virtual environment with:

```shell
uv sync
```
