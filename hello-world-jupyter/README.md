# Hello World Jupyter

A minimal Jupyter notebook project managed with `uv`.

## Run it

```bash
uv sync
uv run jupyter lab
```

Then open `hello_world.ipynb` in JupyterLab. In PyCharm, select
`hello-world-jupyter/.venv/bin/python` as the notebook kernel.
